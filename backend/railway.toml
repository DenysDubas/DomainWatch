[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Healthcheck only on the `api` service (Dashboard → Deploy → Path: /up).
# Worker and scheduler have no HTTP server — do NOT enable healthcheck there.
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
